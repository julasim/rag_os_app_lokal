# Secrets
.env
*.env.local

# Daten
/data/
/backups/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# React-Build-Output (wird vom Docker-Build generiert, nicht ins Repo)
app/ui_static/

# Node
app/frontend/node_modules/
app/frontend/dist/

# IDE
.vscode/
.idea/
*.swp

# Claude Code (lokale Tooling-Ordner, nicht fürs Repo)
.claude/

# OS
.DS_Store
Thumbs.db

# --- Lokaler nativer Umbau (rag-os-app-lokal) ---
# Vault-Index/Cache (LanceDB + corpus.sqlite Snapshots), lokale App-DBs
.ragos/
*.sqlite
*.sqlite-wal
*.sqlite-shm
*.db
# Test-Vault / lokale Daten
/vault/
# PyInstaller / Inno-Setup Build-Artefakte (NUR generierte Teile — die Quell-
# Build-Dateien build/*.spec/*.iss/*.ps1/*.py + build/assets/ragos.ico bleiben im Repo)
/build/models/
/build/redist/
/dist/
/installer_output/
# Modell-/HF-Caches, falls lokal abgelegt
/models/

# graphify: generierter, regenerierbarer Wissensgraph (lokal, nicht ins Repo —
# per `/graphify` jederzeit neu baubar, 0 Tokens/AST). Auch der Streu-Cache unter app/.
graphify-out/
app/graphify-out/

# Build-Logs (transient)
build/*.log
